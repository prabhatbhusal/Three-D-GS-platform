# Node API base URL the Next app calls (see src/lib/api.js).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Only set this for a sub-path deploy (e.g. GitHub Pages). Empty = root.
NEXT_PUBLIC_BASE_PATH=
